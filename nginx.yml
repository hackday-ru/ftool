# strategy: apache exist
- name: nginx-brand-new
  host: all
  tasks:
 	- shell: echo "hi!"
#	- include: check-apache-tls.yml
 	  when: apache_port.stdout != ''







# strategy: apache not exist, we're main 
- name: nginx-frontend-server
  host: all
  tasks:
	- include: nginx-tls-install.yml
	- shell: echo "hi!" 
	- shell: echo "hi!"
 	  when: apache_port.stdout == ''
